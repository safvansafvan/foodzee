import 'package:flutter/material.dart';

class FromBarnyard extends StatelessWidget {
  const FromBarnyard({super.key});

  @override
  Widget build(BuildContext context) {
    return const Column(
      children: [
        Center(
          child: Text('from the barynard'),
        )
      ],
    );
  }
}
