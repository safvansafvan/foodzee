import 'package:flutter/material.dart';

class SaladsAndSoups extends StatelessWidget {
  const SaladsAndSoups({super.key});

  @override
  Widget build(BuildContext context) {
    return const Column(
      children: [
        Center(
          child: Text('salads and soups'),
        )
      ],
    );
  }
}
