import 'package:flutter/material.dart';

class FromIndia extends StatelessWidget {
  const FromIndia({super.key});

  @override
  Widget build(BuildContext context) {
    return const Column(
      children: [
        Center(
          child: Text('from the India'),
        )
      ],
    );
  }
}
